services:
  zoaholic:
    container_name: zoaholic
    build: .
    restart: always
    ports:
      - 8001:8000
    volumes:
      # 注意：首次部署前请先在宿主机创建空文件 `touch api.yaml`，否则 Docker 会将其自动创建为目录
      - ./api.yaml:/home/api.yaml
      - ./data:/home/data
    environment:
      - TZ=Asia/Shanghai
      - DB_TYPE=sqlite
      - DB_PATH=/home/data/stats.db
      # - DATABASE_URL=postgresql://user:pass@host:5432/dbname
      # - CONFIG_URL=http://file_url/api.yaml  # 可选：远程配置
